## Overview

This is a terminal application designed to make managing several branches easier when working with Git by separating branches out into separate Spaces. Each Space contains a clone of the chosen repositories so that you can easily switch between branches and work in parallel.